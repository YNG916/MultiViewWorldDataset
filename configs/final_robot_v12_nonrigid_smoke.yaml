extends: final_robot_preview.yaml
profile: smoke

dataset:
  schema_version: "1.2.0"

configuration_sampling:
  nonrigid_fallback: true
  nonrigid_changed_objects: 2

generation:
  worker_progress_active_stage_timeout_s: 7200
