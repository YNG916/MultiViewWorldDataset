extends: default.yaml
profile: smoke
dataset:
  scene_limit: 1
  accepted_configurations_per_scene: 1
  accepted_episodes_per_configuration: 1
  frames: 10
intervention:
  type_weights:
    rigid_relocation: 1.0
    articulation: 0.0
    state_change: 0.0
robot:
  use_final_robot: false

